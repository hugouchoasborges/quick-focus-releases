# QuickFocus 1.1.2

## Highlights

- Saving filter as workspace now automatically navigates to the newly created saved-search workspace (including `Ctrl+Enter` flow).
- Fixed recurring task duplication on startup when reminder date is already the current day.
- Updated repository maintenance rule: installer generation is now required only during new version releases.
