# QuickFocus 1.8.40

## Highlights

- Tasks now support progress targets, so a single task can track incremental progress like `0/5` with inline `-` and `+` controls.
- The task context menu includes `Set Progress Target` and `Remove Progress Target`.
- Progress values are persisted immediately in SQLite and recurring tasks reset their current progress when the next occurrence is created.
