# QuickFocus 1.1.30a

## Highlights

- Popup de selecao de workspaces destaca `Today` em verde e `Upcoming` em laranja claro.
- Menu de contexto reorganizado com `Set Deadline` e `Set Deadline As...` abaixo de `Add Note` com separador dedicado.
- Traducoes PT ajustadas para dinamicos: `Hoje` e `Futuras`.
