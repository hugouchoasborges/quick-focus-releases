# QuickFocus 1.0.11

- Added local safeguard script to ensure `QuickFocus-Setup.exe` is always present in `releases/<version>/` before release tagging.
- `scripts/package-installer.ps1` now executes this safeguard automatically after installer generation.
