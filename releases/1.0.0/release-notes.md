# QuickFocus 1.0.0

- Add in-app updater with `Settings > Updates`, manual check, startup prompt, and installer fallback.
- Add sync transfer progress visualization during `Sync Now` with download/upload file counters.
- Add release-channel controls in updater (`Install Alpha Releases`, `Install Beta Releases`).
- Update defaults: language English, hotkey `Ctrl + Shift + Alt + W`.
