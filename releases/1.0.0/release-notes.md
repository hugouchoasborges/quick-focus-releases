# QuickFocus 1.0.0

- Migrate local task persistence from JSON to SQLite (`tasks.db`).
- Improve Google Drive sync with per-entity files and deterministic merge behavior.
- Add backup and migration support for local data.
