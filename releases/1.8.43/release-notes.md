# QuickFocus 1.8.43

## Highlights

- A `Show video` button appears in the main window while a `YT Music` session is active. It restores the minimized mini player and brings it to the foreground without restarting playback.
- Fixed installer packaging when a local Google OAuth client file is included.
