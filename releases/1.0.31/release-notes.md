# QuickFocus 1.0.31

## Highlights

- Added a red top shortcut button `Overdue (x)` between the Pomodoro icon and the `G` indicator.
- The shortcut visibility and count reuse the exact same overdue dynamic workspace logic, so it appears only when overdue tasks exist.
- Clicking the shortcut opens the dynamic `Overdue` workspace directly.
