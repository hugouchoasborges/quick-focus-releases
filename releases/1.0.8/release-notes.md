# QuickFocus 1.0.8

- Fixed sync regressions where completed tasks could be reverted after `Sync Now`.
- Persisted cloud sync download cache across app restarts to avoid repeated full remote downloads.
- Stabilized sync payload metadata and project/workspace apply behavior to avoid visual task disappearance after sync.
