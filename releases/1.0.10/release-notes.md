# QuickFocus 1.0.10

- Fixed sync reversion where a task moved to another workspace could return to the previous workspace after `Sync Now`.
- Synced local payload generation now uses the same metadata normalization path as save flow.
- Added extra diagnostics for task location conflicts and move metadata updates.
