# QuickFocus 1.6.25

## Highlights

- Notes: editor de hyperlink agora atualiza o seletor de linhas a partir do `Display Text`, incluindo substituicao de seletor existente em links de arquivo.
- Notes: suporte ampliado de seletores em links de arquivo (`[N]`, `[A:B]`, `[:B]`, `[A:]`, `[-N]`, `[-N:]`, `[:-N]`) e atalho `Ctrl+K` para editar link sob o cursor quando nao ha selecao.
