# QuickFocus 1.0.9

- Enabled direct project switching with `F1..F12` (without workspace modifier).
- Removed project shortcut hint labels from the project popup list.
- Kept project/workspace selection popups interactive at the same time while modifier is held.
