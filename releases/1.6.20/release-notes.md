# QuickFocus 1.6.20

## Highlights

- Novo workspace dinamico automatico `Recently Created`/`Criado Recentemente` com suporte a `All Projects`.
- Selecao de tarefa com tint aplicado em toda a linha (nao apenas borda), mantendo consistencia visual por tema.
- Toast de selecao (`X tasks selected`) agora atualiza em tempo real conforme a contagem e some ao limpar selecao.
