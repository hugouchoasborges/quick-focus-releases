# QuickFocus 1.1.5

## Highlights

- Completed tasks now keep `URL`, `Attachments`, and `Notes` icons visually dimmed (alpha `0.8`) while remaining clickable.
- Workspace selection popup now highlights the keyboard-focused option with a green selection state.
- Fixed `Sync Now` removing a newly created TAG after cloud merge; TAG settings now follow deterministic LWW (`UpdatedAtUtc` + `deviceId`).
