# QuickFocus 1.0.19

- Added automatic dynamic workspace `TODOS`/`ALL` as the last item in `Dynamic`, listing all non-removed tasks from the current project.
- Improved `Ctrl + F` behavior: pressing `Esc` now cancels/clears the active search, matching the `X` button in the search popup.
