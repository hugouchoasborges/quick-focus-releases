# QuickFocus 1.0.21

## Summary

- Stable patch release focused on default settings polish and workspace popup consistency.

## Changed

- New default baseline: `Dark` theme, `110%` scale, `Show Tag Names` enabled, `Show Workspace Number` disabled.
- Default TAG ordering now starts with `Blocked` at the top.
- Workspace picker now shows task totals for normal workspaces as `[N] Name (Count)`.

## Technical

- Release artifacts regenerated and validated for distribution.
